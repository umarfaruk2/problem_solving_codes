#include <bits/stdc++.h>
using namespace std; 

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    int m, n;
    cin >> m >> n;

    int sq = (m * n) / 2;

    cout << sq << "\n";    
    return 0;
}